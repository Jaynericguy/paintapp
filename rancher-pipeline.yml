apiVersion: apps/v1
kind: Deployment
metadata:
  name: paintapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: paintapp
  template:
    metadata:
      labels:
        app: paintapp
    spec:
      containers:
        - name: paintapp
          image: jaynericguy/paintapp-django-armv8:latest
          ports:
            - containerPort: 8000
 
---

apiVersion: v1
kind: Service
metadata:
  name: paintapp-service
spec:
  selector:
    app.kubernetes.io/name: paintapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
 
---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: paintapp-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  rules:
    - host: junder.ddns.net
      http:
        paths:
          - path: /paintapp
            pathType: Prefix
            backend:
              service:
                name: paintapp-service
                port:
                  number: 80
  tls:
    - hosts:
      - junder.ddns.net